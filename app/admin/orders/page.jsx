import { AdminWorkspacePage } from '@/lib/adminPage';

export const metadata = {
  title: 'Orders | Nawal Admin',
};

export default function Page() {
  return <AdminWorkspacePage view="orders" />;
}
