import { AdminWorkspacePage } from '@/lib/adminPage';

export const metadata = {
  title: 'Settings | Nawal Admin',
};

export default function Page() {
  return <AdminWorkspacePage view="settings" />;
}
